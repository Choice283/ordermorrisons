# AI Placeholder Images

Everything in this folder is AI-generated stock-style food photography,
NOT real photos of Morrison's actual dishes. They're used to fill out the
menu visually for pitch/demo purposes while real photos aren't available yet.

Before this site is presented as showing Morrison's real food (i.e. before
any real launch), every image referenced from this folder in `menu.ts`
should be replaced with a real photo, following the same pattern used for
`/public/images/gallery-*.jpg` (real photos already pulled from her site).
